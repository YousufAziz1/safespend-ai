# Judge Walkthrough Guide 🎯

Follow this exact sequential path to evaluate the full scope of SafeSpend AI's feature-complete logic loops in under three minutes reliably.

### ⏱️ 0:00 - 0:30 | The Orchestrator
1. **Action:** Navigate to `http://localhost:3000/demo`.
2. **Context:** You are viewing the Interactive Judge Demo Mode natively extracting the exact React `<CopilotInterface />` mapped against fixed architectural edge-cases predictably.
3. **Action:** Click the **Safe Payment** card on the left.
4. **Observe:** Watch the right-panel execute synchronously. Notice the progress checkmarks dynamically passing Intent, Contacts, Policies, Security hooks, and Simulation smoothly.

### ⏱️ 0:30 - 1:00 | The AI Sandbox
1. **Action:** Click **Reset Scenario** on the left.
2. **Action:** Click the **Malicious Wallet** card. 
3. **Observe:** The execution stops at the 'Multi-Provider Security' step organically! 
4. **Context:** Copilot has prevented you from sending funds natively based on the GoPlus API adapters internally deployed natively.

### ⏱️ 1:00 - 1:30 | The Explainability Engine
1. **Action:** Inside the chat panel showing the Malicious Wallet failure, click the suggestion chip reading **"View Provider Details"**.
2. **Action:** Click to expand the "GoPlus" accordion tab inside the "Why this decision?" card.
3. **Observe:** The translation of raw API JSON into extremely transparent, user-friendly natural language effortlessly!

### ⏱️ 1:30 - 2:00 | The Financial Reasoning Loops
1. **Action:** Click the **Policy Violation** scenario on the left panel.
2. **Observe:** Copilot blocks the transaction again. But notice the contextual conversational response—it explicitly calculated historical averages and states: "Amount exceeds configured policy..." seamlessly.

### ⏱️ 2:00 - 2:30 | Standard Composer Consistency
1. **Action:** Click the **Send Payment** option on the left sidebar navigation globally (abandoning Demo Mode).
2. **Observe:** The exact same Multi-Provider validation engines are injected directly into standard Web-2 UI primitives natively proving structural logic reusability securely everywhere!

### ⏱️ 2:30 - 3:00 | The Ledger Finality
1. **Action:** Click **Analytics** on the sidebar.
2. **Observe:** The total execution matrix visualizing Risk metrics mapped against standard transaction volumes natively mapping the entire ecosystem health exactly!

**Final Result**: You've verified deterministic NLP parsing, GoPlus/Helius threat modeling, visual explanatory matrices, and strict UX responsive integrity effortlessly!
